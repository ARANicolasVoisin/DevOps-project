# Linux distribution

If you're running the container on Linux disrtibution, you must uncomment the line **DNSStubListener** in the file **/etc/systemd/resolved.conf**. Otherwise you have an error message.
