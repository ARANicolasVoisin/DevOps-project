acl aclname {
	IP adress of your network;
};

options {
	forwarders {
		1.1.1.1; # Vous pouvez mettre le DNS que vous souhaiteez, ici c'est cloudflare
	};
	allow-query { aclname; };
};

zone "your domain name" IN {
	type master;
	file "/etc/bind/your-domain-name.zone";
};
